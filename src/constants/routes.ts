export enum ROUTES {
  LOGIN = 'login',
  VERIFY = 'verify',
  HOME = 'home',
  MAPS = 'maps',
  CATEGORIES = 'categories',
  SERVICES = 'services',
  ENTRIES = 'entries',
  BOOKING = "booking"
}
